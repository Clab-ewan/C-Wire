#include <stdio.h>
#include <stdlib.h>
#include "avl_tree.h"

int main() {
    AVLNode *root = NULL;
    FILE *file = fopen("tmp/lv_all.csv", "r");
    


    // N'oubliez pas de libérer la mémoire allouée pour l'arbre

    return EXIT_SUCCESS;
}